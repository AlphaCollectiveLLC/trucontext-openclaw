{
  "id": "trucontext-openclaw",
  "name": "TruContext Memory",
  "description": "Persistent knowledge graph memory for all OpenClaw agents, powered by TruContext",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "tcApiBaseUrl": {
        "type": "string",
        "description": "TruContext API base URL",
        "default": "https://api.trucontext.ai"
      },
      "cronSchedule": {
        "type": "string",
        "description": "Cron schedule for daily maintenance (default: 2am)",
        "default": "0 2 * * *"
      },
      "memoryDays": {
        "type": "number",
        "description": "Days of memory files to harvest on provision",
        "default": 7
      },
      "promptInjection": {
        "type": "boolean",
        "description": "Inject TC recall context into every agent turn via before_prompt_build",
        "default": true
      },
      "recallLimit": {
        "type": "number",
        "description": "Max recall results to inject per turn",
        "default": 5
      }
    }
  }
}
